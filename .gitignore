# --- venvs (we have several, all heavy) ---
env/py*-navsim*/
env/logs/
.venv/

# --- third party clones (we pin them via download scripts, not git submodules
#     for now — too heavy and we patch them) ---
third_party/

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- data (NEVER commit dataset blobs) ---
data/
*.db
*.pkl
*.pt
*.ckpt

# --- editor / OS ---
.idea/
.vscode/
.DS_Store
*.swp
